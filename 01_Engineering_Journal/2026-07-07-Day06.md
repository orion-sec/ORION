# ORION Engineering Journal – Day 6

## Goal

Improve the IOC Extractor by making the code cleaner, more reusable, and more professional.

## Work Completed

- Added IOC summary dashboard.
- Added IOC counts for each IOC type.
- Added `None found.` handling when no indicators are detected.
- Added URL cleanup using `rstrip()`.
- Removed duplicate IOCs using `set()`.
- Created reusable function `display_results()`.
- Created reusable function `display_summary()`.
- Refactored repeated output code into function calls.
- Confirmed the refactored script produces the same output as before.

## Python Concepts Learned

- Functions using `def`
- Function parameters
- Function calls
- Indentation and scope
- `if items:` logic
- Code refactoring
- Abstraction
- Reusable code

## Security Engineering Lesson

IOC extraction requires both detection and cleanup. Raw regex results may capture extra punctuation, duplicates, or malformed values, so security automation must normalize and validate extracted data before presenting it to analysts.

## Test Result

Tested with:

```text
User visited https://evil.com from IP 8.8.8.8. Another connection to https://evil.com. Email attacker@evil.com. Hash d41d8cd98f00b204e9800998ecf8427e.